<canvas id="werByAgeGroupCanvas" ></canvas>
<script>
  window.addEventListener('load', async () => {
    const canvas = document.getElementById('werByAgeGroupCanvas').getContext('2d')
    const response = await axios.get('/ivr/werByAgeGroup')
    const rawData = response.data
    let groups = ['<=35', '>35']
    
    const chartData = {
      labels: groups,
      datasets: [
        {
          backgroundColor: groups.map(t => ChartHelper.colorAmazon()),
          borderWidth: 0,
          data: filterByGroups(rawData, 'AMAZON', groups),
          label: 'Amazon Connect'
        },
        {
          backgroundColor: groups.map(t => ChartHelper.colorDialogflow()),
          borderWidth: 0,
          data: filterByGroups(rawData, 'DIALOGFLOW', groups),
          label: 'Google Dialogflow'
        },
        {
          backgroundColor: groups.map(t => ChartHelper.colorTwilio()),
          borderWidth: 0,
          data: filterByGroups(rawData, 'TWILIO', groups),
          label: 'Twilio Voice'
        }
      ]
    }
    const chartConfiguration = ChartHelper.bar('Word Error Rate by Age Group', chartData, false, 2)
    _.set(chartConfiguration, 'options.plugins.datalabels.font.size', '10')
    _.set(chartConfiguration, 'options.scales.yAxes[0].ticks.max', 0.6)
    //_.set(chartConfiguration, 'options.legend.labels.padding', '30')
    window.werByDomainChart = new Chart(canvas, chartConfiguration)
  })

  function filterByGroups(resultsForPlatform, platform, groups) {
    return groups.map((group) => {
      return resultsForPlatform[platform].find(result => {
        return result.age_group_35 === group
      }).WER
    })
  }
</script>
